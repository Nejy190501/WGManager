package com.example.wgmanager.ui.navigation

// All screens in the app — used as currentScreen state value
enum class AppScreen {
    SPLASH, LOGIN, WG_FINDER,
    DASHBOARD, SHOPPING, CLEANING, CREW, CALENDAR,
    MEAL_PLANNER, VAULT, REWARDS, ANALYTICS,
    BLACKBOARD, PROFILE, SYSTEM_PANEL,
    RECURRING_COSTS, WALL_OF_FAME, GUEST_PASS,
    SMART_HOME, ONBOARDING
}
